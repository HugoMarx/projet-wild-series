{% extends 'base.html.twig' %}

{% block title %}Edit Program
{% endblock %}

{% block body %}
	<h1>Edit Program</h1>

	{{form_start(form)}}
	{{form_widget(form)}}
    <button class="btn-sm btn-secondary" type='submit'>Modifier</button>
	{{form_end(form)}}

	<a href="{{ path('app_actor_index') }}">back to list</a>
{% endblock %}
